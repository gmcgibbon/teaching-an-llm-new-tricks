#!/usr/bin/env ruby
# frozen_string_literal: true

root = File.join(__dir__, "..")

Dir.chdir(root) do
  require "bundler"

  Bundler.setup

  require_relative "../app"

  module App
    extend Mcp
  end

  App.server.start
end
